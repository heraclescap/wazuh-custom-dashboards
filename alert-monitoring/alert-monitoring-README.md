# 📡 Alert Monitoring Dashboard

> **Wazuh Dashboard** — Real-time alert monitoring and triage, with classification by severity level and distribution of events across the entire agent fleet.

---

## 🎯 Purpose of the dashboard

This dashboard is designed to be **the first view an SOC analyst opens** at the start of a shift or during rapid triage. It answers three fundamental questions at a glance:

1. How many critical alerts have been generated over the selected period?
2. How are these alerts distributed by severity level?
3. How has the volume of alerts changed over time?

It is aimed at both Level 1 analysts who need a quick indicator, and Level 2/3 analysts who wish to contextualise an investigation.

---

## 🗂️ Pattern Index used

```
wazuh-alerts-*
```

This dashboard uses Wazuh’s main index, which aggregates all alerts generated by the correlation engine (ossec), Syscheck, Vulnerability Detector, and third-party integrations (Suricata, VirusTotal, Microsoft Defender, etc.).

---

## 📸 General overview of the dashboard

<!-- SCREENSHOT TO BE PLACED HERE -->
> 📌 **[Insert a screenshot of the full dashboard here]**
> Recommendation: 24-hour or 7-day view with live data, 1920×1080 resolution.

---

## 🧩 Detailed visualisations

### 1. Critical Alerts — Metric counter

**Visualisation type:** Metric (numeric counter)

**Fields used:** `count()` (without additional filters; counts all alerts for the period)

**Operational role:**

This counter displays the total number of alerts generated within the selected time window (by default, used as an overview of the current period). It acts as a **criticality gauge**: a figure significantly higher than the usual baseline should immediately attract attention.

In practice, this dashboard allows you to:
- Quickly detect an abnormal increase in alert volume (potential brute-force spike, malware propagation, rule storm)
- Verify that data collection is working (a zero or an abnormally low number may indicate an agent or pipeline issue)
- Serve as a starting point for an investigation: the figure should be mentally compared to the baseline from the previous week

The custom label **‘Alerts’** has been retained to ensure readability for non-technical audiences (management team, reporting).

<!-- SCREENSHOT TO BE PLACED HERE -->
> 📌 **[Insert a screenshot of the ‘Critical Alerts’ panel here]**

---

### 2. Breakdown by Severity Level — Pie chart (Donut)

**Visualisation type:** Pie chart (Donut)

**Fields used:**
- Metric: `count()`
- Bucket/Dimension: `rule.level` (top 10, sorted by descending count)

**Operational role:**

In Wazuh, the `rule.level` field is an integer ranging from 0 to 15 that reflects the severity of a rule according to Wazuh’s internal classification:
- Levels **0–6**: Informative / Low severity (routine alerts, logins, minor configuration changes)
- Levels **7–11**: moderate (failed authentication attempts, modified files, anomalies)
- Levels **12–15**: critical/high (rootkits, injections, privilege escalations, confirmed attacks)

This doughnut chart allows you to see at a glance the **breakdown of the alert load**: if the majority is concentrated at levels 12+, the SOC must prioritise immediately. If the majority is at levels 3–7, the day will be more focused on reviewing rules and false positives.

The legend displayed at the top of the chart, showing the percentage per bracket, facilitates quick communication during team meetings or a SOC stand-up.

During investigations, this panel is used to decide how to filter Discover: if a particular level is unusually dominant, a `rule.level: 12` filter is created in Discover to view the associated alerts.

<!-- SCREENSHOT TO BE PLACED HERE -->
> 📌 **[Insert a screenshot of the ‘Breakdown by severity level’ panel here]**

---

## 📥 Importing the dashboard

### Prerequisites

- Wazuh 4.x operational with at least one active agent
- OpenSearch Dashboards (or Kibana 7.10+) connected to the cluster
- `wazuh-alerts-*` index pattern already created in Stack Management → Index Patterns

### Import procedure

1. From the side menu, go to **Stack Management** → **Saved Objects**
2. Click **Import** (button at the top right)
3. Select the file `Alert_Monitoring.ndjson`
4. If there is a conflict (an object with the same ID already exists), select **Overwrite**
5. Confirm the import
6. Navigate to **Dashboards** → the **‘Alert Monitoring’** dashboard should appear in the list

### ⚠️ Adjust the base URL if necessary

The `.ndjson` file contains references to a target host in the `fieldFormatMap` (fields `data.url`, `data.virustotal.permalink`, `data.vulnerability.reference`). If these fields are formatted as clickable links, check that the base URL matches your instance. To adjust it, open the `.ndjson` file in a text editor and replace `YOUR_WAZUH_HOST` with your IP or FQDN.

---

## 🔧 Recommended customisation

**Add a minimum level filter**: If your environment generates a lot of noise at low levels (0–6), you can add a persistent filter `rule.level >= 7` directly in the dashboard’s filter bar, then save the dashboard with this filter active.

**Change the default time window**: The dashboard opens without a set time window. You can set a default window (e.g. ‘Last 24 hours’) by editing the dashboard options.

**Enhance with a timeline**: A time series (`date_histogram` on `timestamp`) is a natural addition to this dashboard. It would allow you to see peaks in alerts over time.

---

## 🧪 Tested on

| Component | Version |
|---------- -|---------|
| Wazuh | 4.x |
| OpenSearch Dashboards | 2.x |
| Index Pattern | `wazuh-alerts-*` |

---

## 📄 Licence

MIT — free to use, modify and redistribute.
